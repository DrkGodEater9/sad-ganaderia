# Copia este archivo a ".env" en la raíz del repo si necesitas apuntar la
# app a un backend distinto de "http://localhost:8000" (por ejemplo, antes
# de publicar el build de producción en un hosting).
VITE_API_BASE_URL=http://localhost:8000
